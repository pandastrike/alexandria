# A content cache to store crawled content using Elasticsearch

crawler-cache provides a simple interface to store resources downloaded from the web.
It uses Elasticsearch to index downloaded content.